"use strict"

/**
 * Yields the items of the iterator mapped through a given transformer.
 *
 * @ntime O(n)
 * @dspace O(1)
 * @example Basic Usage
 *
 * ```javascript
 * [1,2,3]::map(function () {
 *   return this * 2;
 * }) // yields 2, 4, 6
 * ```
*/
export function map (
) {
};
