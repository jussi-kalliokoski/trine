"use strict"

/**
 * Yields the items that don't qualify the condition.
 *
 * @ntime O(n)
 * @dspace O(1)
 * @example Basic Usage
 *
 * ```javascript
 * [1,2,3,4]::drop(function () {
 *   return this % 2 === 0;
 * }) // yields 1, 3
 * ```
*/
export function drop (
) {
};
