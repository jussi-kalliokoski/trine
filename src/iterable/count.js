"use strict"

/**
 * Yields the number of items in the iterator.
 *
 * @ntime O(n)
 * @dspace O(1)
 * @example Basic Usage
 *
 * ```javascript
 * [1,2,3]::count() // yields 3
 * ```
*/
export function count (
) {
};
