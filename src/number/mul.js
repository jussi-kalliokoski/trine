"use strict";

/**
 * Returns the multiplication of two numbers.
 *
 * @this {number}
 * @example Basic Usage
 *
 * ```javascript
 * 8::mul(-2) // returns -16
 * 4::mul(3) // returns 12
 * 5::mul(1) // returns 5
 * ```
*/
export function mul (
    b : number,
) : number {
};
