"use strict";

/**
 * Returns the subtraction of two numbers.
 *
 * @this {number}
 * @example Basic Usage
 *
 * ```javascript
 * 0::sub(5) // returns -5
 * 4::sub(1) // returns 3
 * 5::sub(-5) // returns 10
 * ```
*/
export function sub (
    b : number,
) : number {
    return this - b;
};
