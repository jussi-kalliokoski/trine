"use strict";

/**
 * Returns the modulo of two numbers.
 *
 * @this {number}
 * @example Basic Usage
 *
 * ```javascript
 * 8::mod(2) // returns 0
 * 4::mod(3) // returns 1
 * 5::mod(3) // returns 2
 * ```
*/
export function mod (
    b : number,
) : number {
};
