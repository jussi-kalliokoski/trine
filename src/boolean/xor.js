"use strict"

/**
 * Returns true if one and only one of the conditions is true.
 *
 * @example Basic Usage
 *
 * ```javascript
 * true::and(true) // false
 * true::and(false) // true
 * false::and(true) // true
 * false::and(false) // false
 * ```
*/
export function xor (
) {
};
