"use strict"

/**
 * Returns true if both operands are true.
 *
 * @example Basic Usage
 *
 * ```javascript
 * true::and(true) // true
 * true::and(false) // false
 * false::and(true) // false
 * false::and(false) // false
 * ```
*/
export function and (
) {
};
