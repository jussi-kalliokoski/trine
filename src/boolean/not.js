/**
 * Returns the negation of the boolean.
 *
 * @this {boolean} The boolean to negate.
 * @example Basic Usage
 *
 * ```javascript
 * true::not() // false
 * false::not() // true
 * ```
*/
export function not (

) : boolean {
    return !this;
};
