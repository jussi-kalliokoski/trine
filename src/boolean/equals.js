"use strict"

/**
 * Returns the strict equality of the two operands.
 *
 * @example Basic Usage
 *
 * ```javascript
 * "foo"::equals("bar") // false
 * "bar"::equals("bar") // true
 * ```
*/
export function equals (
) {
};
